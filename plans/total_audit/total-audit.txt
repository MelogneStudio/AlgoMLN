 Confirmed anomalies (I read the code myself)

  A1 — MAX_DAILY_LOSS is a silent no-op in live trading. engine.rs:540-574
  check_risk_breach divides realized loss by broker_initial_cash(), which returns 0.0 for any non-PaperBroker target. The if initial
  > 0.0 guard then skips the check entirely for DhanBroker. So the one risk control CLAUDE.md invariant 9d calls "hard,
  non-negotiable" for going live never actually fires at runtime. The live gate (guard.rs) requires it to be declared, but it's never
  enforced. Highest-impact finding.

  A2 — MAX_ORDERS can block stop-loss / take-profit exits. engine.rs:507-516
  session_orders is incremented on every successful execute (BUY, SELL, and SL/TP SELL ALL), and the max_orders check sits at the top
  of check_risk_breach before the action type is inspected. Once the cap is hit, a stop-loss SELL is rejected with MaxOrdersReached,
  leaving a position open with its safety net disabled. Contradicts the spirit of invariant 12 ("SL/TP is a safety net"). Exits
  should be exempt from the order cap.

  A4 — enable/disable error path permanently loses the plugin. registry.rs:178-206 (same at :218+)
  The real plugin is mem::replaced out with EmptyPlugin before on_enable().await?. If on_enable returns Err, ? returns early and the
  moved-out plugin is dropped — the registry slot keeps EmptyPlugin with status Loaded forever, while the UI still lists it. A plugin
  that errors on enable effectively unloads itself silently. Needs the plugin restored to the slot on the error path.

  A5 — daily_realized_loss is a dead write. engine.rs:549-556
  Written on every call with a comment claiming it avoids recomputation, but check_risk_breach always recomputes realized_loss() from
  the broker. The field is never read. Harmless, but the comment is misleading and the "daily" name implies a reset that never
  happens.

  Reported by subagents, plausible but lower-confidence

  B1 — No per-candle market-hours gate in the live tick loop. session.rs / guard.rs
  is_market_open is checked only at preflight. A session started at 15:29 keeps assembling candles and can attempt orders after
  15:30. Flagged as known limitation "L4" but it's a real post-close order hazard.

  B3 — WASM CPU cap is armed but never driven. wasm_runtime.rs:150-160
  set_epoch_deadline(1) is set but nothing calls increment_epoch() (no watchdog), and there's no fuel and async_support(false). An
  infinite loop in a plugin lifecycle export hangs the host thread. The primary containment mechanism doesn't contain.

  B4 — GatedLiveExecutionApi gate-5/6 read broker-global state, not the fetched session. execution.rs:282-294
  Gate 1 pulls session from the slot, but gate 5 checks self.broker.cancel_token() and gate 4 self.broker.is_stale(). In
  single-session Phase 8 the broker token is the active session's, so not currently exploitable — but it's a latent inconsistency
  that breaks the moment multi-session lands, and gate 6 already reads session.broker while 4/5 read self.broker.

  B5 — Trade-log durability + poison. trade_log.rs:52-60
  Append uses flush() not fsync (a crash can lose the last audited trades), and a panic while holding the mutex poisons it, disabling
  all further logging for the session. For an immutable audit log of real orders, both are worth hardening.

  Resolved

  A1 — Fixed: check_risk_breach now reads the initial cash from
  StrategyInstance::initial_cash (threaded through from LiveSession::start with
  DEFAULT_LIVE_INITIAL_CASH = 1_000_000.0 INR for the Tauri binary, or
  whatever the caller passes). The old broker_initial_cash() downcast that
  returned 0.0 for any non-PaperBroker target is gone. Guarded by
  risk_max_daily_loss_fires_for_non_paper_broker and a stderr warning emitted
  when initial_cash <= 0.0 so the user notices the cap won't fire.

  A2 — Fixed: check_risk_breach now short-circuits to Ok(()) for any exit
  action (SELL, SELL ALL, including strategy-level STOP_LOSS / TAKE_PROFIT
  synthetic closes) before incrementing session_orders. session_orders is
  incremented only on successful entry (BUY) execute, so a failed order does
  not consume the cap and exits never consume it. Guarded by
  risk_max_orders_does_not_block_stop_loss,
  risk_max_orders_does_not_block_take_profit,
  risk_max_orders_does_not_count_or_block_rule_driven_sell, and
  risk_max_orders_exhausted_then_re_armed_after_exit.

  B1 — Fixed: LiveSession's tick loop now runs is_market_open(candle_close_ist,
  holiday_calendar) per candle before pushing the candle into
  engine.plan_candle. A session started at 15:29 will not submit an order
  after 15:30. The closed candle still lands in candle_history so indicator
  windows stay continuous across weekend gaps. Guarded by
  test_gate_skips_weekend_candle, test_gate_skips_post_close_candle,
  test_gate_skips_holiday_candle.

  B2 — Fixed: CandleAssembler::feed now returns Vec<Candle> and emits a
  zero-volume gap candle for every minute strictly between the closed
  minute and the new tick's minute (flat at the last seen close), so the
  engine sees a contiguous minute-by-minute series across feed gaps.
  CandleAssembler::flush() emits the in-progress partial minute's candle
  and is called from the cancel.cancelled() branch and the RecvError::Closed
  branch of the live tick loop, so a stop or feed shutdown mid-minute
  preserves the partial candle's prices and volume. The flushed candle
  flows through the same B1-gated LiveSession::append_candle helper the
  regular tick body uses. Guarded by test_gap_emits_zero_volume_minutes,
  test_multi_minute_gap_emits_one_per_missing_minute,
  test_flush_emits_partial_candle, and test_flush_before_any_tick_is_none
  in candle_assembler.rs::tests (plus the existing B1 regression tests
  staying green proves the append_candle refactor is structurally sound).

  A3 — Fixed: == / != comparisons in `compare` now use a relative tolerance (1e-9) combined with an absolute floor.
  This ensures that equalities are correctly evaluated for large values (e.g. prices around 20,000) where bitwise
  equality is too strict. Guarded by `test_compare_float_tolerance` in `engine.rs`.

  Clean / no issue found

  - DSL parser/validator/lexer: no panics on malformed input, ranges (0,100] correct, wire boundary
  (BacktestResultWire/PaperTradeWire) fully aligned with the TS contract — no missing/renamed fields.
  - Lock discipline in commands/live.rs: no parking_lot guard held across .await (all await-spanning locks are tokio::sync::Mutex).
  Clean.
  - on_candle/plan_candle equivalence, determinism (BTreeMap usage), and SL/TP bypass-of-trigger-state are all correct.

Status as of 2026-08-21

  Resolved (committed):
    A1, A2, B1, B2 — see "Resolved" block above.
    Phase 8 Tasks #1–#4 — plan_candle / execute_intent split (C2), H3 cancellation
    plumbing, GatedLiveExecutionApi plugin order gateway (Task #4).

  B3 — Fixed: each WasmPlugin now owns an EpochWatchdog background thread
  (wasm_runtime.rs) that calls Engine::increment_epoch() every
  EPOCH_TICK_MS = 100 ms, and call_lifecycle re-arms
  store.set_epoch_deadline(LIFECYCLE_CPU_BUDGET_TICKS = 50) before every
  lifecycle export (~5 s CPU budget). An infinite loop in _algomln_on_load
  / _algomln_on_enable / _algomln_on_disable now traps instead of hanging
  the host thread. The watchdog is spawned in on_load and stopped+joined
  (via Drop) in on_unload so it never outlives the plugin. Guarded by
  epoch_watchdog_traps_infinite_loop and epoch_watchdog_stops_on_drop in
  wasm_runtime.rs::tests. The old inert set_epoch_deadline(1) one-shot is
  gone.

  Open (lower-confidence, not yet patched):
    A4 — enable/disable error path permanently loses the plugin (registry.rs:178-206,
    218+). The real plugin is mem::replaced with EmptyPlugin before on_enable().await?;
    an Err from on_enable drops the plugin silently. Fix: restore on the error path.
    A5 — daily_realized_loss is a dead write (engine.rs:549-556). Never read; comment
    misleading; no "daily" reset. Harmless but worth removing or wiring up.
    B4 — GatedLiveExecutionApi gates 4/5 read broker-global state, not the fetched
    session (execution.rs:282-294). Latent inconsistency that breaks under multi-session.
    Fix: pass the resolved session through and read from it.
    B5 — Trade-log durability + poison (trade_log.rs:52-60). Append uses flush() not
    fsync; a panic while holding the mutex poisons it. Fix: fsync after append + a
    parking_lot Mutex that doesn't poison.

  Out of scope / deferred:
    - Phase 8 Task #5 (W1/W2/W5 wiring comments + batch refresh_realized_loss).
    - Phase 8 Task #6 (L2 / L8 pub-field hardening).
    - Phase 9 multi-session (full lock release across execute; per-session broker
      state in GatedLiveExecutionApi).

  Test baseline: cargo test --lib → 341 passed, 1 ignored, 0 failed.